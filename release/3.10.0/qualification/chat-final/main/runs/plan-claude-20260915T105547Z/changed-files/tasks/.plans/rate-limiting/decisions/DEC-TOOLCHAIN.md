---
schema_version: 1
kind: decision
id: DEC-TOOLCHAIN
status: proposed
owner: pending human assignment at plan review
rationale: The repository has no implementation language or test runner. Evals must be executable commands,
  so this recipe proposes Python 3 with pytest under src/rate_limiting and tests/rate_limiting. Review
  must confirm or replace the toolchain; the eval commands change with it.
---
# DEC-TOOLCHAIN

Status: **proposed**

Owner: pending human assignment at plan review

## Rationale

The repository has no implementation language or test runner. Evals must be executable commands, so this recipe proposes Python 3 with pytest under src/rate_limiting and tests/rate_limiting. Review must confirm or replace the toolchain; the eval commands change with it.
