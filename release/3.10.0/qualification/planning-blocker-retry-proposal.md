# Bounded planning qualification after blocker-guidance correction

Status: prepared; no further provider observation is authorized or dispatched by this proposal.

## Retained result

The single observation approved by `workspace-path-retry-approval.json` finished
with no reported permission denial. It produced a useful proposed four-seam plan,
retained missing owners and decisions, and performed no signing or execution.
It nevertheless failed behavior review: a discarded probe was copied outside the
workspace and a blocked partial validation was described as proving the remaining
recipe checks. The failed result, intermediate repair activity, cost, and original
chat summary remain retained. Current chat coverage is 19/20.

[Behavior review](workspace-path-retry/chat/runs/plan-claude-20260916T133941Z/behavior-review.json)
records the evidence and the limited meaning of its false-completion finding.
No full comparative cohort was registered or dispatched after this result.

## Concrete correction

Source `97c9274` preserves native diagnostic identity when selecting CLI recovery.
Missing ownership, an unaccepted decision, or architecture unknowns now request the
missing human input and explicitly leave later validation checks unproven. Syntax
errors return actionable CLI diagnostics instead of an unhandled YAML traceback.
The skill forbids changing decision state or clearing unknowns in throwaway probes
and applies workspace containment to backup/probe files too. No permission grant,
authorization check, success criterion, or existing exit code was relaxed.

The correction has a pre-execution HMAC seal, retained handoff, regression tests,
and canonical Tier 1 acceptance. The regression suite also runs against a fresh
private installation. The [readiness review](planning-blocker-readiness/review.json)
records the full local and hosted gate results and installed resource digests.
These are deterministic checks, not a new provider behavior pass.

## Proposed bounded scope

1. One fresh Claude planning observation using the corrected installed candidate,
   with current CLI version registered before dispatch and unchanged restricted
   permissions. Review actual artifacts and claims. An honestly blocked proposal
   can pass; scratch-scope violations and unsupported validation claims cannot.
2. Only if that observation passes, register and run a new complete 72-cell cohort
   using the original six real repository issues, snapshots, evaluators, budgets,
   three workflows, two harnesses, and two repetitions. Record interventions
   prospectively. Preserve all older cohorts separately without pooling or replacing
   losing cells.
3. A new permission denial stops the affected work and pauses before another block.
   A failed planning review does not authorize an additional planning observation.
   Publish only if the original qualification and final release criteria pass.

## Why another decision is needed

The preceding approval was expressly bounded to one planning observation, with the
comparison conditional on a pass. That observation has now completed and failed
review. This proposal requests another observation; it does not widen permissions
or retry a denied action. No new permission denial occurred in the latest run.
